# Dependencies
client/node_modules/
server/node_modules/

# Environment
client/.env
server/.env

# Logs
*.log
*.err

# Lingkungan kerja lokal opencode (jangan ikut commit)
.opencode/

# Tool tunneling lokal (cloudflared/ngrok binary + log)
tools/

# Build
client/dist/

# OS
.DS_Store
Thumbs.db
*.stackdump

# VSCode
.vscode/

# Temporary
*.tmp

# PID file & skrip dev lokal (bukan bagian repo)
*.pid
start-dev.bat
start-tunnel.bat
# Sisa file media era penyimpanan lokal (sudah tidak dipakai lagi,
# media sekarang tersimpan di Cloudinary). Dibuat agar tidak ikut dicommit.
server/uploads/

# Cloudinary backup (jangan commit berisi secret)
server/.env.cloudinary.bak

.vercel
.env*
